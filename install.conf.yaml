- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.vim: vim/
    ~/.git-authors: pairing/git-authors
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.bash_profile: bash_profile
    ~/.bash_prompt: bash_prompt
    ~/.proxy: proxy
    ~/.ssh/config: ssh/config
    ~/.bash_profile: bash_profile
    ~/Library/Application\ Support/Karabiner/private.xml: pairing/karabiner/private.xml
    ~/.atom: atom/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [mkdir -p ~/dev, Creating ~/dev]
  - [cp pairing/karabiner/private.xml ~/Library/Application\ Support/Karabiner/private.xml, copying karabiner keymaps]

